// export const toTwoDecimalPlaces = (value) => {
//   const parsedValue = parseFloat(value);
//   if (!isNaN(parsedValue)) {
//     return parsedValue.toFixed(2); // Format to two decimal places
//   }
//   return "0.00"; // Return default '0.00' if value is not a valid number
// };
